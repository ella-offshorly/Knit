package Base;

import org.openqa.selenium.WebDriver;

//Setting Selenium WebDriver to variable 'Driver' so it can be used in the other classes.
public class BaseUtil {

    public static WebDriver Driver;
}
